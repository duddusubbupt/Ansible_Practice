---
- hosts: all
  become: yes
  tasks:
    - name: Install apache2 on ubuntu
      apt:
        name: apache2
        state: present
      when: ansible_os_family == "Debian"

    - name: Start apache2 on ubuntu
      service:
        name: apache2
        state: started
      when: ansible_os_family == "Debian"